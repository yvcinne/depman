# Fichier de configuration des dépendances
# Généré automatiquement le 2026-05-02 15:34:20 par gen-deps
# Format : nom_paquet >= version_minimale

[project:depman]
# git  (non installé — version minimale à définir manuellement)
# curl  (non installé — version minimale à définir manuellement)
# python3  (non installé — version minimale à définir manuellement)
# nodejs  (non installé — version minimale à définir manuellement)
# gcc  (non installé — version minimale à définir manuellement)
# make  (non installé — version minimale à définir manuellement)
